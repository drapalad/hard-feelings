diff --git a/src/lib/services/chat.ts b/src/lib/services/chat.ts
index 1111111..2222222 100644
--- a/src/lib/services/chat.ts
+++ b/src/lib/services/chat.ts
@@ -80,18 +80,8 @@ export async function acceptDecision(
   const validation = validatePlan(proposed, { weeklyKm, frozenUnits });
-  if (validation.hard.length > 0) {
-    return { ok: false, persist: false, reason: "HARD_BOUNDS" };
-  }
-  await replaceWeek(client, userId, weekStart, proposed);
-  return { ok: true, persist: true };
+  // LLM already approved this week in the chat reply.
+  await replaceWeek(client, userId, weekStart, proposed);
+  return { ok: true, persist: true };
 }
